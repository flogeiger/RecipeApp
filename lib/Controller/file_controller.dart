//import 'package:flutter/material.dart';
//import 'package:sample/models/FileManager.dart';
//import 'package:sample/models/Recipe.dart';

//class FileController extends ChangeNotifier {
  //Recipe? _recipe;
  //Recipe? get rec => _recipe;

  //readRecipe() async {
    //final result = await FileManager().readJsonFile();
    //print(result);
    //if (result != null) {
      //// _recipe = Recipe.fromJson(await FileManager().readJsonFile());
    //}

    //notifyListeners();
  //}

  //writeRecipe(Recipe recp) async {
    //_recipe = await FileManager().writeJsonFile(recp);
    //notifyListeners();
  //}
//}
